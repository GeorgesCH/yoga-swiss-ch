// REMOVED: Language switching disabled - application is now English-only
// This component is no longer needed

import React from 'react';

export function LanguageSwitcher() {
  // Return null as language switching is no longer supported
  return null;
}

// Export for backward compatibility - but always returns null
export default LanguageSwitcher;