// Mount teachers circle routes
app.route('/teachers-circle', teachersCircleApp);

// Mount retreat requests routes
app.route('/retreat-requests', retreatRequestsApp);

// Mount settings routes (if not already mounted)
// app.route('/make-server-f0b2daa4/settings', settingsRouter);

// Mount organizations routes (if not already mounted)  
// app.route('/make-server-f0b2daa4/organizations', organizationsRouter);
